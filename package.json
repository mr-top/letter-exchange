{
  "name": "letter-exchange",
  "version": "1.0.0",
  "description": "Letter Sharing Web App",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd front_end && npm install && npm run build && cd ../back_end && npm install && node app.js"
  },
  "author": "",
  "license": "ISC"
}
